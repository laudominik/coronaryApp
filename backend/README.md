# Backend of CoronaryApp
Setup venv
```
python -m venv .venv
source .venv/bin/activate
```
(max. python3.11)
Get deps.
```
pip install -r requirements.txt
pip install ..
```
Run
```
python manage.py runserver
```